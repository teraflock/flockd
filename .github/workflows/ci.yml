name: ci

on:
  push:
    branches: [main]
  pull_request:

env:
  GO_VERSION: "1.25"

jobs:
  # The proto contract repo is a sibling checkout (go.mod replace ../proto).
  # Each job checks both repos out side by side.

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with: { path: flockd }
      - uses: actions/checkout@v7
        with: { repository: teraflock/proto, path: proto }
      - uses: actions/setup-go@v7
        with: { go-version: "${{ env.GO_VERSION }}", cache-dependency-path: flockd/go.sum }
      - name: Lint
        working-directory: flockd
        run: |
          go install github.com/mgechev/revive@v1.15.0
          $(go env GOPATH)/bin/revive -formatter friendly ./...

  test:
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        # Governor idle/yield logic is platform-behavioral: run its suite on
        # macOS and Windows runners too (SPEC §A3).
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
        with: { path: flockd }
      - uses: actions/checkout@v7
        with: { repository: teraflock/proto, path: proto }
      - uses: actions/setup-go@v7
        with: { go-version: "${{ env.GO_VERSION }}", cache-dependency-path: flockd/go.sum }
      - name: unit tests
        working-directory: flockd
        run: go test ./...
      - name: race (concurrency-heavy packages)
        if: matrix.os == 'ubuntu-latest'
        working-directory: flockd
        run: go test -race ./internal/governor/ ./internal/tunnel/... ./internal/runtime/... ./internal/localapi/
      - name: smoke (standalone end-to-end)
        if: matrix.os != 'windows-latest'
        working-directory: flockd
        run: ./scripts/smoke.sh

  build:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - { goos: darwin, goarch: arm64 }
          - { goos: darwin, goarch: amd64 }
          - { goos: linux, goarch: amd64 }
          - { goos: linux, goarch: arm64 }
          - { goos: windows, goarch: amd64 }
    steps:
      - uses: actions/checkout@v7
        with: { path: flockd }
      - uses: actions/checkout@v7
        with: { repository: teraflock/proto, path: proto }
      - uses: actions/setup-go@v7
        with: { go-version: "${{ env.GO_VERSION }}", cache-dependency-path: flockd/go.sum }
      - name: cross-build
        working-directory: flockd
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          go build ./...
          go build -o /dev/null ./cmd/flockd
          go build -o /dev/null ./cmd/flock

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version: "22", cache: npm, cache-dependency-path: web/package-lock.json }
      - name: build dashboard
        working-directory: web
        run: |
          npm ci
          npm run build
      - name: ensure committed dist is not stale (informational)
        run: git status --porcelain web/dist || true

  release:
    # Tag builds produce (unsigned, draft) release artifacts.
    # TODO(keys): cosign + notarization + Authenticode before public release.
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [test, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with: { path: flockd, fetch-depth: 0 }
      - uses: actions/checkout@v7
        with: { repository: teraflock/proto, path: proto }
      - uses: actions/setup-go@v7
        with: { go-version: "${{ env.GO_VERSION }}", cache-dependency-path: flockd/go.sum }
      - uses: goreleaser/goreleaser-action@v6
        with:
          workdir: flockd
          args: release --clean
        env:
          # The formula push targets teraflock/homebrew-tap, which the
          # workflow-scoped GITHUB_TOKEN cannot write; TAP_GITHUB_TOKEN is
          # a PAT with write access to that repo. Falls back so tag builds
          # still publish release artifacts without it.
          GITHUB_TOKEN: ${{ secrets.TAP_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
